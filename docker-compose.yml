services:
  mcp-sfw-fmc:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: mcp-sfw-fmc
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
