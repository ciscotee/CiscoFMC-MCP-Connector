# Core FMC settings (single-FMC mode)
FMC_BASE_URL=https://<fmc-host>
FMC_USERNAME=<api-user>
FMC_PASSWORD=<password>
FMC_VERIFY_SSL=false
FMC_TIMEOUT=30
# Optional: set if you need to pin a specific FMC domain UUID
# FMC_DOMAIN_UUID=<domain-uuid>

# Multi-FMC profile mode: point to the profiles directory and optional default profile id
# FMC_PROFILES_DIR=profiles
# FMC_PROFILE_DEFAULT=fmc-north-south

# Server transport settings
# MCP_TRANSPORT=http          # stdio | http
# MCP_HOST=0.0.0.0
# MCP_PORT=8000

# HTTP auth (Bearer token). If set, clients must send Authorization: Bearer <token>.
# MCP_AUTH_TOKEN=<base64-or-random-token>

# Logging (single-FMC mode or Docker env)
# LOG_LEVEL=INFO            # overall app logging
# HTTPX_TRACE=0             # set 1/true to log FMC request URLs + status codes
# HTTPX_LOG_LEVEL=WARNING   # httpx/httpcore verbosity (default WARNING)
